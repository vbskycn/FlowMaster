# 更新记录

本项目遵循语义化版本。正式发布前的修改记录在“未发布”章节。

## 未发布

暂无。

## 1.1.21 - 2026-09-15

### 安全性

- 生产服务改为专用、无登录的 `flowmaster` 系统账号运行；程序与配置保持 root 只写，并加强 systemd 文件系统、进程、权限和 capability 隔离，同时严格限制 unit 与实际进程只能使用专用主组和可信 `vnstat` 附属组。
- 安装器会拒绝接管不符合约束的同名用户或组，并在任何 PM2 迁移前拒绝符号链接或特殊文件形式的旧 `.env`，避免泄露配置或迁移后静默失效。
- 管理接口在未配置令牌时默认拒绝匿名访问；页面移除管理操作入口，CSP 禁止内联脚本，动态错误和表格内容统一安全转义。
- 安装、更新、卸载、备份和恢复共用可信维护锁；路径、文件所有者、链接、归档拓扑、成员数量及压缩/展开体积均执行严格校验。
- vnstat 备份增加强制外部摘要、内部清单、数据库可读性校验和持久化屏障；恢复使用原子目录切换，并在故障或信号中断时回滚原数据和服务状态。

### 修复

- 部署与卸载改为完整事务，精确保存并恢复程序目录、unit、控制命令、服务启用/运行状态及本次新增的服务身份；systemd 启用、禁用与重启失败或状态不符会显式阻断发布，并核对最终沙箱配置、附属组、开机自启状态及主进程 UID/GID/Groups。
- systemd 部署健康检查改为本次 unit 随机实例令牌绑定，精确核对 Node exe 与 argv，并要求同一 PID 和重启计数跨 `RestartSec=3` 连续稳定 4 秒；安装器同时明确要求 systemd 247 或更高版本。
- 加固旧 PM2 离线迁移的阶段记录和中断恢复，避免过滤保存清单后重复恢复、再次进入 TreeKill 或遗留不可控的 FlowMaster 进程。
- 备份和恢复不再把一次瞬时 `active` 当作启动成功，也不再把带残留进程的 `inactive/failed` 当作停服完成；停服需同时确认 `MainPID`、`ControlPID` 为零且受信任 unit cgroup 内无进程，常驻服务需连续保持健康主进程，短暂启动后崩溃会触发失败与数据回滚。
- vnstat 命令增加全局并发和队列上限；接口列表、非实时统计和实时采样支持 single-flight，正在采样的接口不可被驱逐后重复创建，实时失败采用有限指数退避并显式标记陈旧样本。
- 不再虚构 `eth0`；无已建库接口时前后端均保持空状态，也不会轮询空接口路径。
- 修复英文 vnstat 日统计的 `date` 表头未翻译、旧版 `MM/DD/YY` 日期行列错位、极小非零流量被再次舍入为零，以及接口切换或请求乱序导致旧数据回写的问题。
- 实时解析兼容 vnstat 原始英文输出、低速 `bit/s` 及 bit/Byte 两类速率单位，并按 SI/IEC 语义正确换算为 Mb/s。
- 修复深色主题部分卡片文字对比不足、长采样间隔下页面误报实时数据过期等显示问题。

### 功能与体验

- 非实时统计在兼容原 `data` 文本的基础上新增 MiB 精度 `series`；实时响应新增 `stale` 与 `ageMs`。
- 前端脚本从页面模板拆分为可测试模块，增加请求取消、图表清理、页面隐藏暂停、响应式布局及无障碍语义。
- 缓存改为受条目数和内存双重约束的 LRU，并限制限流客户端桶数量，降低长期运行的内存风险。

### 依赖

- 运行依赖和锁文件更新至当前最新稳定版，移除已不使用的 Bootstrap Icons，并保持 Node.js 18、22 和 24 兼容。

### 文档与测试

- 重构 README，将 API、运维和贡献说明拆分为独立文档；补齐国内代理、固定版本校验、服务身份、配置、备份恢复、回滚和 PM2 故障处理边界。
- 新增 Node/API/前端回归、Shell 静态安全断言、归档攻击与资源耗尽用例、部署/卸载事务故障注入，以及 Debian root/systemd + PM2 5.4.3 真实恢复测试。

## 1.1.20 - 2026-08-29

### 修复

- 修复 PM2 首次恢复后执行 `pm2 delete flowmaster` 再次触发 PM2 5.x TreeKill 递归 `ps`、造成僵尸进程风暴并进入重复恢复的问题。
- PM2 迁移改为单次“有界停止 unit → 离线过滤主/备清单 → 按需恢复其他应用”，并临时清空 `ExecStop=pm2 kill`，在安装器迁移路径中绕开旧 TreeKill；保存清单只有 FlowMaster 时 PM2 保持停止。
- 兼容 1.1.19 已写入的临时/持久 systemd drop-in，避免故障现场升级时因配置不一致被拒绝。

### 测试

- Debian/systemd + PM2 5.4.3 真实故障注入扩展为无响应、健康和仅 FlowMaster 三种迁移场景，并验证不执行 TreeKill `ps`、其他应用及 stopped 状态保持、主备清单精确过滤和空清单停机行为。

## 1.1.19 - 2026-08-29

### 修复

- 安装器可在无需重启服务器的情况下恢复由独立 root `pm2-*.service` 管理的无响应 PM2，阻断其持续制造 `ps` 僵尸进程。
- PM2 恢复增加精确影响列表和 `RECOVER-PM2` 确认、安装与恢复互斥锁、systemd/cgroup/进程启动时间校验、硬超时、原子 drop-in、恢复前备份和应用集合验证。
- 迁移后同时过滤 `dump.pm2` 与 `dump.pm2.bak` 中精确名为 `flowmaster` 的条目，避免旧应用在 PM2 重启时复活，同时保留相似名称、其他应用和 stopped 状态。
- 对非独立 systemd PM2、不可信清单、CLI 与 unit 版本不一致及停止边界被覆盖的场景安全拒绝，避免直接杀 daemon 产生孤儿或重复应用。

### 已知问题

- PM2 首次恢复后仍可能因在线移除 FlowMaster 再次触发 TreeKill；受影响环境应升级到 1.1.20 或更高版本。

### 依赖

- npm 锁定依赖均对齐最新稳定版，并将 GitHub Actions 的 checkout 与 Node.js 环境动作更新到最新稳定版、固定完整提交摘要。

### 测试

- 新增 Debian/systemd + PM2 5.4.3 真实故障注入，覆盖非 systemd 安全拒绝、后置配置冲突、连续两次 SIGSTOP 恢复、在线与停止应用保持、精确迁移、主备清单和僵尸回归。

## 1.1.18 - 2026-08-28

### 修复

- 默认进程管理器由全局 PM2 迁移到 systemd，避免旧 PM2 指标采集引发 `ps` 僵尸进程风暴。
- 安装器增加僵尸进程预检、旧 PM2 有界迁移、下载与冒烟硬超时，并确保退出时回收临时进程组。
- 实时 vnstat 采集改为按接口请求驱动和单飞去重，移除终端 ANSI 控制序列，避免全接口启动突发和重复命令。
- 修正 vnstat `KiB`/`B` 等单位归一化时的数值换算，避免小流量列被放大 1024 倍或更多。
- 修正前端网络速率的十进制 SI 换算、缓存超大条目拒绝、限流桶数量上限、CORS 错误状态和 API 404 响应。

### 依赖

- 精确锁定最新稳定版 Express 5.2.1、Vue 3.5.42、Chart.js 4.5.1、Axios 1.20.0、Bootstrap 5.3.8、Bootstrap Icons 1.13.1、CORS 2.8.6 和 dotenv 17.4.2。
- 移除 nodemon，开发模式改用 Node.js 内置 `--watch`，消除 Node.js 18 下的依赖引擎警告。

### 测试

- 增加 Express 5 的 CORS、限流、静态资源和 API 回归，以及安装器端口占用、请求超时、进程组清理、僵尸阈值和 PM2 无响应测试。
- Debian 实机覆盖 systemd 全新安装、安全更新、启动失败回滚、真实 vnstat 采样和保留 vnstat 数据的卸载流程。

## 1.1.17 - 2026-08-28

### 安全性

- 安装、更新和卸载流程不再删除 vnstat 历史数据库。
- vnstat 命令改用参数化执行，并增加超时、输出上限、接口和日期校验。
- 增加安全响应头、API 限流、CORS 白名单和可选管理令牌。
- 移除第三方页面统计脚本，前端依赖改为本地锁定版本。

### 修复

- 修复实时缓存重复展示旧采样、采集命令重叠及切换网卡后混入旧数据的问题。
- 修复 Gb/s 换算、日期范围重复换算、页脚版本不渲染和事件监听无法清理的问题。
- 统一周期缓存时间和统计输出归一化逻辑。

### 工程化

- 提交 npm 锁文件，增加 Node 内置测试、前端脚本语法检查和 GitHub Actions。
- 增加可回滚部署、部署前冒烟测试及带校验和的一致性备份恢复流程。
- 补充项目级协作、安全、测试和发布规则。
